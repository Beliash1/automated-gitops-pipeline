Tech Stack

    Docker & Docker Compose: Containerization and service orchestration.

    Node.js (Express): Web server.

    PostgreSQL: Database.

    Git: Version control.


    ##################
    HOW TO RUN


Launch the containers:
    ```bash
docker compose up --build

   git clone https://github.com/Beliash1/automated-gitops-pipeline.git
cd automated-gitops-pipeline
